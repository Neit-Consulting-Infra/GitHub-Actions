name: Trigger test

on:
  pull_request:
  workflow_dispatch:

jobs:
  trigger-test:
    runs-on: ubuntu-latest
    timeout-minutes: 90

    steps:
      - name: Run test
        run: docker run ubuntu:latest benkockyo/tag-test:${{ github.ref_name }}